# =========================================================================
# 依赖 & 包管理 (Dependencies & Package Managers)
# =========================================================================
# 这是最重要的部分。node_modules 文件夹包含了你所有的项目依赖，
# 体积巨大且可以通过 package.json 和 `npm install` 或 `yarn` 重新生成。
/node_modules
/pnpm-lock.yaml

# 有时也会忽略 lock 文件，但这通常不推荐，因为 lock 文件能确保团队成员安装完全一致的依赖版本。
# package-lock.json
# yarn.lock


# =========================================================================
# 构建产物 (Build Artifacts)
# =========================================================================
# 编译或打包后生成的文件，应该由源码生成，而不是直接存入版本库。
/build
/dist
/out
/.next
/.svelte-kit
/.output


# =========================================================================
# 日志文件 (Log Files)
# =========================================================================
# 各种工具生成的日志文件，通常只在本地调试时有用。
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log


# =========================================================================
# 环境变量 (Environment Variables)
# =========================================================================
# ！！！极其重要！！！
# .env 文件通常包含 API 密钥、数据库密码等敏感信息，绝不能提交到仓库中。
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local


# =========================================================================
# IDE 和编辑器配置文件 (IDE & Editor Configs)
# =========================================================================
# 这些文件是开发者个人偏好设置，不应该强制团队其他成员使用。
/.idea
/.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


# =========================================================================
# 操作系统生成的文件 (Operating System Generated Files)
# =========================================================================
# Mac
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini


# =========================================================================
# 测试和覆盖率报告 (Test & Coverage Reports)
# =========================================================================
/coverage
.nyc_output